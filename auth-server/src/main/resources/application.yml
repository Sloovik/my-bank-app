server:
  port: 9000

spring:
  application:
    name: auth-server
  cloud:
    config:
      import-check:
        enabled: false

security:
  jwt:
    key-id: my-bank-app-rsa-key-2024

eureka:
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
